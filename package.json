{
  "devDependencies": {
    "cypress": "^13.6.1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "jsonwebtoken": "^9.0.2"
  },
  "scripts": {
    "start": "node ./server/server.js",
    "build": "cd client && npm install && npm run heroku-postbuild && cd ../server && npm install"
  }
}
